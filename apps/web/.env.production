# Fallback caso a variável não esteja setada no dashboard da Vercel.
# A Vercel injeta NEXT_PUBLIC_API_URL como env var real, que tem precedência.
NEXT_PUBLIC_API_URL=https://delivery-route-optimizer-api.fly.dev
